apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test
  labels:
    app: test
spec:
  ingressClassName: traefik
  rules:
    - host: test.gihp4c.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test
                port:
                  number: 80
