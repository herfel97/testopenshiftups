apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test
  namespace: gihp4c
spec:
  ingressClassName: traefik
  rules:
    - host: test.gihp4c.edu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test
                port:
                  number: 80