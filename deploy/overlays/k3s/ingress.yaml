apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test
  labels:
    app: test
spec:
  ingressClassName: traefik
  rules:
    - host: test.54.237.238.39.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test
                port:
                  number: 80
